# API Phase 1 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Rust/Axum + async-graphql APIサーバーをDocker Compose + LocalStackで動かし、ユーザー・犬・散歩のCRUD GraphQL APIを実装する。

**Architecture:** Axumで単一の `/graphql` エンドポイントを公開。Cognito JWT検証ミドルウェアで認証。SeaORMでPostgreSQL操作、aws-sdk-dynamodbでWalkPoints操作。ServiceレイヤーがEntityをGraphQL型に変換する。

**Tech Stack:** Rust, Axum, async-graphql, SeaORM, PostgreSQL, DynamoDB (LocalStack), S3 (LocalStack), Amazon Cognito JWT

**Spec:** `docs/superpowers/specs/2026-03-21-walking-dog-app-design.md`

---

## File Structure

```
apps/api/
├── compose.yml                         # Modify: PostgreSQL + LocalStack + cognito-local + api
├── Cargo.toml                          # Create: ワークスペース設定
├── .env.example                        # Create: 環境変数サンプル
├── Dockerfile                          # Create: プロダクション用 (マルチステージ)
├── Dockerfile.dev                      # Create: 開発用 (cargo watch ホットリロード)
├── src/
│   ├── main.rs                         # Create: Axumサーバー起動
│   ├── config.rs                       # Create: 環境変数設定
│   ├── error.rs                        # Create: AppError型
│   ├── auth/
│   │   └── mod.rs                      # Create: Cognito JWT検証ミドルウェア + TEST_MODE bypass
│   ├── graphql/
│   │   ├── mod.rs                      # Create: GraphQLスキーマ組み立て
│   │   ├── context.rs                  # Create: GraphQLコンテキスト(user_id等)
│   │   ├── types/
│   │   │   ├── mod.rs
│   │   │   ├── user.rs                 # Create: User GraphQL型 (#[Object] with dogs resolver)
│   │   │   ├── dog.rs                  # Create: Dog, BirthDate, BirthDateInput, WalkStats GraphQL型
│   │   │   └── walk.rs                 # Create: Walk, WalkPoint GraphQL型 (#[Object] with dogs resolver)
│   │   ├── query/
│   │   │   ├── mod.rs                  # Create: QueryRoot
│   │   │   ├── user.rs                 # Create: me query
│   │   │   ├── dog.rs                  # Create: dog query
│   │   │   └── walk.rs                 # Create: walk, myWalks queries
│   │   └── mutation/
│   │       ├── mod.rs                  # Create: MutationRoot
│   │       ├── user.rs                 # Create: updateProfile mutation
│   │       ├── dog.rs                  # Create: createDog, updateDog, deleteDog, generateDogPhotoUploadUrl
│   │       └── walk.rs                 # Create: startWalk, addWalkPoints, finishWalk
│   ├── db/
│   │   └── mod.rs                      # Create: DatabaseConnection pool
│   ├── entities/
│   │   ├── mod.rs
│   │   ├── users.rs                    # Create: SeaORM User entity
│   │   ├── dogs.rs                     # Create: SeaORM Dog entity
│   │   ├── walks.rs                    # Create: SeaORM Walk entity
│   │   └── walk_dogs.rs                # Create: SeaORM WalkDog entity
│   ├── services/
│   │   ├── mod.rs
│   │   ├── user_service.rs             # Create: ユーザーCRUDロジック
│   │   ├── dog_service.rs              # Create: 犬CRUDロジック (完全実装)
│   │   ├── walk_service.rs             # Create: 散歩ロジック
│   │   └── walk_points_service.rs      # Create: DynamoDB WalkPoints操作
│   └── aws/
│       ├── mod.rs
│       └── client.rs                   # Create: DynamoDB/S3クライアントビルダー (endpoint override対応)
├── migration/
│   ├── Cargo.toml                      # Create: sea-orm-migration
│   └── src/
│       ├── main.rs
│       ├── lib.rs
│       ├── m20260321_000001_create_users.rs
│       ├── m20260321_000002_create_dogs.rs
│       ├── m20260321_000003_create_walks.rs
│       └── m20260321_000004_create_walk_dogs.rs
└── tests/
    ├── common/
    │   └── mod.rs                      # Create: テストDB/サーバーセットアップ
    ├── test_health.rs
    ├── test_user.rs
    ├── test_dog.rs
    └── test_walk.rs
```

---

## Task 1: ローカル開発環境のセットアップ

**Files:**
- Modify: `apps/api/compose.yml`
- Create: `apps/api/.env.example`
- Create: `apps/api/Dockerfile.dev`

> **方針**: ローカル開発はすべてDockerで完結させる。Rust API も `api` サービスとして compose に含め、ソースコードをボリュームマウントして `cargo watch` でホットリロードする。Cognito ローカルエミュレーターには `jagregory/cognito-local` を使う。

- [ ] **Step 1: Dockerfile.devを作成 (開発用)**

```dockerfile
# apps/api/Dockerfile.dev
FROM rust:1-slim-bookworm
RUN apt-get update && apt-get install -y pkg-config libssl-dev && rm -rf /var/lib/apt/lists/*
RUN cargo install cargo-watch
WORKDIR /app
# 依存クレートをキャッシュするため先にCargo.tomlだけコピーしてビルド
COPY Cargo.toml Cargo.lock ./
COPY migration/Cargo.toml ./migration/
RUN mkdir -p src migration/src && \
    echo "fn main() {}" > src/main.rs && \
    echo "" > migration/src/lib.rs && \
    echo "fn main() {}" > migration/src/main.rs && \
    cargo build 2>/dev/null || true
# ソースコードはボリュームマウントで提供するため COPY しない
CMD ["cargo", "watch", "-x", "run"]
```

- [ ] **Step 2: compose.ymlにすべてのサービスを追加**

```yaml
# apps/api/compose.yml
services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: walking_dog
      POSTGRES_PASSWORD: password
      POSTGRES_DB: walking_dog_dev
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  localstack:
    image: localstack/localstack:3
    environment:
      SERVICES: dynamodb,s3
      AWS_DEFAULT_REGION: ap-northeast-1
    ports:
      - "4566:4566"
    volumes:
      - localstack_data:/var/lib/localstack

  cognito-local:
    image: jagregory/cognito-local:latest
    ports:
      - "9229:9229"
    volumes:
      - cognito_data:/app/.cognito

  api:
    build:
      context: .
      dockerfile: Dockerfile.dev
    ports:
      - "3000:3000"
    volumes:
      - .:/app                          # ソースコードをマウント
      - cargo_cache:/usr/local/cargo    # cargoキャッシュを永続化
      - target_cache:/app/target        # ビルドキャッシュを永続化
    env_file:
      - .env
    environment:
      DATABASE_URL: postgres://walking_dog:password@postgres:5432/walking_dog_dev
      AWS_ENDPOINT_URL: http://localstack:4566
      COGNITO_ENDPOINT_URL: http://cognito-local:9229
    depends_on:
      - postgres
      - localstack
      - cognito-local

volumes:
  postgres_data:
  localstack_data:
  cognito_data:
  cargo_cache:
  target_cache:
```

> **注意**: `api` サービス内では `DATABASE_URL` のホストが `postgres` (サービス名) になる。ホストから直接接続する場合は `localhost:5432` を使う。

- [ ] **Step 3: .env.exampleを作成**

```ini
# apps/api/.env.example
# ホストから直接実行する場合の設定 (docker compose up api を使う場合は上書きされる)
DATABASE_URL=postgres://walking_dog:password@localhost:5432/walking_dog_dev
AWS_REGION=ap-northeast-1
AWS_ENDPOINT_URL=http://localhost:4566
AWS_ACCESS_KEY_ID=test
AWS_SECRET_ACCESS_KEY=test
DYNAMODB_TABLE_WALK_POINTS=WalkPoints
S3_BUCKET_DOG_PHOTOS=dog-photos
COGNITO_USER_POOL_ID=local_XXXXXXXX
COGNITO_REGION=ap-northeast-1
COGNITO_ENDPOINT_URL=http://localhost:9229
PORT=3000
# TEST_MODE=true  # JWT検証をスキップしてテスト用固定ユーザーを返す (本番では削除)
```

- [ ] **Step 4: .envをコピーしてサービスを起動**

```bash
cd apps/api
cp .env.example .env
docker compose up -d postgres localstack cognito-local
docker compose ps
```

Expected output:
```
NAME                STATUS
postgres            running
localstack          running
cognito-local       running
```

- [ ] **Step 5: APIサービスを起動してログ確認**

```bash
docker compose up api
```

Expected: `Listening on 0.0.0.0:3000` のログが出ること (初回ビルドは数分かかる)

- [ ] **Step 6: コミット**

```bash
cd apps/api
git add compose.yml .env.example Dockerfile.dev
git commit -m "chore(api): add full Docker dev environment with Rust, PostgreSQL, LocalStack, cognito-local"
```

---

## Task 2: Rustプロジェクトの雛形作成

**Files:**
- Create: `apps/api/Cargo.toml`
- Create: `apps/api/src/main.rs`
- Create: `apps/api/src/config.rs`
- Create: `apps/api/src/error.rs`
- Create: `apps/api/migration/Cargo.toml`
- Create: `apps/api/migration/src/main.rs`
- Create: `apps/api/migration/src/lib.rs`

- [ ] **Step 1: ワークスペースCargo.tomlを作成**

```toml
# apps/api/Cargo.toml
[workspace]
members = [".", "migration"]
resolver = "2"

[package]
name = "walking-dog-api"
version = "0.1.0"
edition = "2021"

[dependencies]
axum = { version = "0.7", features = ["macros"] }
tokio = { version = "1", features = ["full"] }
async-graphql = { version = "7", features = ["chrono", "uuid"] }
async-graphql-axum = "7"
sea-orm = { version = "1", features = ["sqlx-postgres", "runtime-tokio-rustls", "macros", "with-uuid", "with-chrono", "with-json"] }
aws-config = { version = "1", features = ["behavior-version-latest"] }
aws-sdk-dynamodb = "1"
aws-sdk-s3 = { version = "1", features = ["behavior-version-latest"] }
aws-types = "1"
jsonwebtoken = "9"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
uuid = { version = "1", features = ["v4"] }
chrono = { version = "0.4", features = ["serde"] }
dotenvy = "0.15"
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
thiserror = "1"
reqwest = { version = "0.12", features = ["json"] }
base64 = "0.22"

[dev-dependencies]
reqwest = { version = "0.12", features = ["json"] }
```

> **注意**: `aws-sdk-s3-presigning` という独立したクレートは存在しない。プレサイニングは `aws-sdk-s3` v1 に内包されている。

- [ ] **Step 2: migration/Cargo.tomlを作成**

```toml
# apps/api/migration/Cargo.toml
[package]
name = "migration"
version = "0.1.0"
edition = "2021"

[lib]
name = "migration"
path = "src/lib.rs"

[dependencies]
sea-orm-migration = { version = "1", features = ["sqlx-postgres", "runtime-tokio-rustls"] }

[[bin]]
name = "migration"
path = "src/main.rs"
```

- [ ] **Step 3: config.rsを作成**

```rust
// apps/api/src/config.rs
#[derive(Clone, Debug)]
pub struct Config {
    pub database_url: String,
    pub aws_region: String,
    pub aws_endpoint_url: Option<String>,
    pub dynamodb_table_walk_points: String,
    pub s3_bucket_dog_photos: String,
    pub cognito_user_pool_id: String,
    pub cognito_region: String,
    /// cognito-local のエンドポイント (例: http://localhost:9229)
    /// 本番環境では None にする
    pub cognito_endpoint_url: Option<String>,
    pub port: u16,
    pub test_mode: bool,
}

impl Config {
    pub fn from_env() -> Self {
        dotenvy::dotenv().ok();
        Self {
            database_url: std::env::var("DATABASE_URL").expect("DATABASE_URL must be set"),
            aws_region: std::env::var("AWS_REGION").unwrap_or_else(|_| "ap-northeast-1".to_string()),
            aws_endpoint_url: std::env::var("AWS_ENDPOINT_URL").ok(),
            dynamodb_table_walk_points: std::env::var("DYNAMODB_TABLE_WALK_POINTS")
                .unwrap_or_else(|_| "WalkPoints".to_string()),
            s3_bucket_dog_photos: std::env::var("S3_BUCKET_DOG_PHOTOS")
                .unwrap_or_else(|_| "dog-photos".to_string()),
            cognito_user_pool_id: std::env::var("COGNITO_USER_POOL_ID")
                .unwrap_or_default(),
            cognito_region: std::env::var("COGNITO_REGION")
                .unwrap_or_else(|_| "ap-northeast-1".to_string()),
            cognito_endpoint_url: std::env::var("COGNITO_ENDPOINT_URL").ok(),
            port: std::env::var("PORT")
                .unwrap_or_else(|_| "3000".to_string())
                .parse()
                .expect("PORT must be a number"),
            test_mode: std::env::var("TEST_MODE")
                .map(|v| v == "true" || v == "1")
                .unwrap_or(false),
        }
    }
}
```

- [ ] **Step 4: error.rsを作成**

```rust
// apps/api/src/error.rs
use thiserror::Error;

#[derive(Debug, Error)]
pub enum AppError {
    #[error("Database error: {0}")]
    Database(#[from] sea_orm::DbErr),
    #[error("Not found: {0}")]
    NotFound(String),
    #[error("Unauthorized: {0}")]
    Unauthorized(String),
    #[error("Bad request: {0}")]
    BadRequest(String),
    #[error("Internal error: {0}")]
    Internal(String),
}

/// AppError を async_graphql::Error に変換する。
/// エラー種別に応じて GraphQL extension code を付与する。
impl From<AppError> for async_graphql::Error {
    fn from(e: AppError) -> Self {
        use async_graphql::ErrorExtensions;
        match e {
            AppError::BadRequest(msg) => {
                async_graphql::Error::new(msg).extend_with(|_, ext| {
                    ext.set("code", "BAD_USER_INPUT");
                })
            }
            AppError::NotFound(msg) => {
                async_graphql::Error::new(msg).extend_with(|_, ext| {
                    ext.set("code", "NOT_FOUND");
                })
            }
            AppError::Unauthorized(msg) => {
                async_graphql::Error::new(msg).extend_with(|_, ext| {
                    ext.set("code", "UNAUTHENTICATED");
                })
            }
            other => async_graphql::Error::new(other.to_string()).extend_with(|_, ext| {
                ext.set("code", "INTERNAL_SERVER_ERROR");
            }),
        }
    }
}
```

- [ ] **Step 5: main.rsの雛形を作成**

```rust
// apps/api/src/main.rs
mod auth;
mod aws;
mod config;
mod db;
mod entities;
mod error;
mod graphql;
mod services;

use axum::{Router, routing::get};
use std::net::SocketAddr;

#[tokio::main]
async fn main() {
    tracing_subscriber::fmt::init();
    let config = config::Config::from_env();
    let addr = SocketAddr::from(([0, 0, 0, 0], config.port));
    tracing::info!("Listening on {}", addr);

    let app = Router::new()
        .route("/health", get(|| async { "ok" }));

    let listener = tokio::net::TcpListener::bind(addr).await.unwrap();
    axum::serve(listener, app).await.unwrap();
}
```

- [ ] **Step 6: ビルド確認**

```bash
cd apps/api
cargo build 2>&1 | head -20
```

Expected: コンパイルエラーなし (警告は許容)

- [ ] **Step 7: コミット**

```bash
git add apps/api/Cargo.toml apps/api/src/main.rs apps/api/src/config.rs apps/api/src/error.rs apps/api/migration/Cargo.toml
git commit -m "chore(api): scaffold Rust project with Axum and async-graphql"
```

---

## Task 3: データベースマイグレーション

**Files:**
- Create: `apps/api/migration/src/lib.rs`
- Create: `apps/api/migration/src/main.rs`
- Create: `apps/api/migration/src/m20260321_000001_create_users.rs`
- Create: `apps/api/migration/src/m20260321_000002_create_dogs.rs`
- Create: `apps/api/migration/src/m20260321_000003_create_walks.rs`
- Create: `apps/api/migration/src/m20260321_000004_create_walk_dogs.rs`

- [ ] **Step 1: migration/src/main.rsを作成**

```rust
// apps/api/migration/src/main.rs
use sea_orm_migration::prelude::*;

#[tokio::main]
async fn main() {
    cli::run_cli(migration::Migrator).await;
}
```

- [ ] **Step 2: migration/src/lib.rsを作成**

```rust
// apps/api/migration/src/lib.rs
pub use sea_orm_migration::prelude::*;

mod m20260321_000001_create_users;
mod m20260321_000002_create_dogs;
mod m20260321_000003_create_walks;
mod m20260321_000004_create_walk_dogs;

pub struct Migrator;

#[async_trait::async_trait]
impl MigratorTrait for Migrator {
    fn migrations() -> Vec<Box<dyn MigrationTrait>> {
        vec![
            Box::new(m20260321_000001_create_users::Migration),
            Box::new(m20260321_000002_create_dogs::Migration),
            Box::new(m20260321_000003_create_walks::Migration),
            Box::new(m20260321_000004_create_walk_dogs::Migration),
        ]
    }
}
```

- [ ] **Step 3: usersテーブルマイグレーションを作成**

```rust
// apps/api/migration/src/m20260321_000001_create_users.rs
use sea_orm_migration::prelude::*;

pub struct Migration;

impl MigrationName for Migration {
    fn name(&self) -> &str { "m20260321_000001_create_users" }
}

#[async_trait::async_trait]
impl MigrationTrait for Migration {
    async fn up(&self, manager: &SchemaManager) -> Result<(), DbErr> {
        manager.create_table(
            Table::create()
                .table(Users::Table)
                .if_not_exists()
                .col(ColumnDef::new(Users::Id).uuid().not_null().primary_key()
                    .extra("DEFAULT gen_random_uuid()"))
                .col(ColumnDef::new(Users::CognitoSub).string().not_null().unique_key())
                .col(ColumnDef::new(Users::DisplayName).string().null())
                .col(ColumnDef::new(Users::AvatarUrl).string().null())
                .col(ColumnDef::new(Users::CreatedAt).timestamp_with_time_zone().not_null()
                    .extra("DEFAULT now()"))
                .to_owned(),
        ).await
    }

    async fn down(&self, manager: &SchemaManager) -> Result<(), DbErr> {
        manager.drop_table(Table::drop().table(Users::Table).to_owned()).await
    }
}

#[derive(Iden)]
enum Users {
    Table, Id, CognitoSub, DisplayName, AvatarUrl, CreatedAt,
}
```

- [ ] **Step 4: dogsテーブルマイグレーションを作成**

```rust
// apps/api/migration/src/m20260321_000002_create_dogs.rs
use sea_orm_migration::prelude::*;

pub struct Migration;

impl MigrationName for Migration {
    fn name(&self) -> &str { "m20260321_000002_create_dogs" }
}

#[async_trait::async_trait]
impl MigrationTrait for Migration {
    async fn up(&self, manager: &SchemaManager) -> Result<(), DbErr> {
        manager.create_table(
            Table::create()
                .table(Dogs::Table)
                .if_not_exists()
                .col(ColumnDef::new(Dogs::Id).uuid().not_null().primary_key()
                    .extra("DEFAULT gen_random_uuid()"))
                .col(ColumnDef::new(Dogs::UserId).uuid().not_null())
                .col(ColumnDef::new(Dogs::Name).string().not_null())
                .col(ColumnDef::new(Dogs::Breed).string().null())
                .col(ColumnDef::new(Dogs::Gender).string().null())
                .col(ColumnDef::new(Dogs::BirthDate).json_binary().null())
                .col(ColumnDef::new(Dogs::PhotoUrl).string().null())
                .col(ColumnDef::new(Dogs::CreatedAt).timestamp_with_time_zone().not_null()
                    .extra("DEFAULT now()"))
                .foreign_key(
                    ForeignKey::create()
                        .from(Dogs::Table, Dogs::UserId)
                        .to(Users::Table, Users::Id)
                        .on_delete(ForeignKeyAction::Cascade),
                )
                .to_owned(),
        ).await
    }

    async fn down(&self, manager: &SchemaManager) -> Result<(), DbErr> {
        manager.drop_table(Table::drop().table(Dogs::Table).to_owned()).await
    }
}

#[derive(Iden)]
enum Dogs { Table, Id, UserId, Name, Breed, Gender, BirthDate, PhotoUrl, CreatedAt }
#[derive(Iden)]
enum Users { Table, Id }
```

- [ ] **Step 5: walksテーブルマイグレーションを作成**

```rust
// apps/api/migration/src/m20260321_000003_create_walks.rs
use sea_orm_migration::prelude::*;

pub struct Migration;

impl MigrationName for Migration {
    fn name(&self) -> &str { "m20260321_000003_create_walks" }
}

#[async_trait::async_trait]
impl MigrationTrait for Migration {
    async fn up(&self, manager: &SchemaManager) -> Result<(), DbErr> {
        manager.create_table(
            Table::create()
                .table(Walks::Table)
                .if_not_exists()
                .col(ColumnDef::new(Walks::Id).uuid().not_null().primary_key()
                    .extra("DEFAULT gen_random_uuid()"))
                .col(ColumnDef::new(Walks::UserId).uuid().not_null())
                .col(ColumnDef::new(Walks::Status).string().not_null()
                    .extra("DEFAULT 'active'"))
                .col(ColumnDef::new(Walks::DistanceM).integer().null())
                .col(ColumnDef::new(Walks::DurationSec).integer().null())
                .col(ColumnDef::new(Walks::StartedAt).timestamp_with_time_zone().not_null())
                .col(ColumnDef::new(Walks::EndedAt).timestamp_with_time_zone().null())
                .foreign_key(
                    ForeignKey::create()
                        .from(Walks::Table, Walks::UserId)
                        .to(Users::Table, Users::Id)
                        .on_delete(ForeignKeyAction::Cascade),
                )
                .to_owned(),
        ).await
    }

    async fn down(&self, manager: &SchemaManager) -> Result<(), DbErr> {
        manager.drop_table(Table::drop().table(Walks::Table).to_owned()).await
    }
}

#[derive(Iden)]
enum Walks { Table, Id, UserId, Status, DistanceM, DurationSec, StartedAt, EndedAt }
#[derive(Iden)]
enum Users { Table, Id }
```

- [ ] **Step 6: walk_dogsテーブルマイグレーションを作成**

```rust
// apps/api/migration/src/m20260321_000004_create_walk_dogs.rs
use sea_orm_migration::prelude::*;

pub struct Migration;

impl MigrationName for Migration {
    fn name(&self) -> &str { "m20260321_000004_create_walk_dogs" }
}

#[async_trait::async_trait]
impl MigrationTrait for Migration {
    async fn up(&self, manager: &SchemaManager) -> Result<(), DbErr> {
        manager.create_table(
            Table::create()
                .table(WalkDogs::Table)
                .if_not_exists()
                .col(ColumnDef::new(WalkDogs::WalkId).uuid().not_null())
                .col(ColumnDef::new(WalkDogs::DogId).uuid().not_null())
                .primary_key(Index::create().col(WalkDogs::WalkId).col(WalkDogs::DogId))
                .foreign_key(
                    ForeignKey::create()
                        .from(WalkDogs::Table, WalkDogs::WalkId)
                        .to(Walks::Table, Walks::Id)
                        .on_delete(ForeignKeyAction::Cascade),
                )
                .foreign_key(
                    ForeignKey::create()
                        .from(WalkDogs::Table, WalkDogs::DogId)
                        .to(Dogs::Table, Dogs::Id)
                        .on_delete(ForeignKeyAction::Cascade),
                )
                .to_owned(),
        ).await
    }

    async fn down(&self, manager: &SchemaManager) -> Result<(), DbErr> {
        manager.drop_table(Table::drop().table(WalkDogs::Table).to_owned()).await
    }
}

#[derive(Iden)]
enum WalkDogs { Table, WalkId, DogId }
#[derive(Iden)]
enum Walks { Table, Id }
#[derive(Iden)]
enum Dogs { Table, Id }
```

- [ ] **Step 7: マイグレーション実行**

```bash
cd apps/api
DATABASE_URL=postgres://walking_dog:password@localhost:5432/walking_dog_dev \
  cargo run --manifest-path migration/Cargo.toml -- up
```

Expected:
```
Applying migration 'm20260321_000001_create_users'
Applying migration 'm20260321_000002_create_dogs'
Applying migration 'm20260321_000003_create_walks'
Applying migration 'm20260321_000004_create_walk_dogs'
```

- [ ] **Step 8: コミット**

```bash
git add apps/api/migration/
git commit -m "feat(api): add database migrations for users, dogs, walks, walk_dogs"
```

---

## Task 4: SeaORM Entityとデータベース接続

**Files:**
- Create: `apps/api/src/db/mod.rs`
- Create: `apps/api/src/entities/mod.rs`
- Create: `apps/api/src/entities/users.rs`
- Create: `apps/api/src/entities/dogs.rs`
- Create: `apps/api/src/entities/walks.rs`
- Create: `apps/api/src/entities/walk_dogs.rs`

- [ ] **Step 1: db/mod.rsを作成**

```rust
// apps/api/src/db/mod.rs
use sea_orm::{Database, DatabaseConnection, DbErr};

pub async fn connect(database_url: &str) -> Result<DatabaseConnection, DbErr> {
    Database::connect(database_url).await
}
```

- [ ] **Step 2: entities/users.rsを作成**

```rust
// apps/api/src/entities/users.rs
use sea_orm::entity::prelude::*;

#[derive(Clone, Debug, PartialEq, DeriveEntityModel)]
#[sea_orm(table_name = "users")]
pub struct Model {
    #[sea_orm(primary_key, auto_increment = false)]
    pub id: Uuid,
    pub cognito_sub: String,
    pub display_name: Option<String>,
    pub avatar_url: Option<String>,
    pub created_at: DateTimeWithTimeZone,
}

#[derive(Copy, Clone, Debug, EnumIter, DeriveRelation)]
pub enum Relation {
    #[sea_orm(has_many = "super::dogs::Entity")]
    Dogs,
    #[sea_orm(has_many = "super::walks::Entity")]
    Walks,
}

impl Related<super::dogs::Entity> for Entity {
    fn to() -> RelationDef { Relation::Dogs.def() }
}

impl Related<super::walks::Entity> for Entity {
    fn to() -> RelationDef { Relation::Walks.def() }
}

impl ActiveModelBehavior for ActiveModel {}
```

- [ ] **Step 3: entities/dogs.rsを作成**

```rust
// apps/api/src/entities/dogs.rs
use sea_orm::entity::prelude::*;

#[derive(Clone, Debug, PartialEq, DeriveEntityModel)]
#[sea_orm(table_name = "dogs")]
pub struct Model {
    #[sea_orm(primary_key, auto_increment = false)]
    pub id: Uuid,
    pub user_id: Uuid,
    pub name: String,
    pub breed: Option<String>,
    pub gender: Option<String>,
    pub birth_date: Option<Json>,
    pub photo_url: Option<String>,
    pub created_at: DateTimeWithTimeZone,
}

#[derive(Copy, Clone, Debug, EnumIter, DeriveRelation)]
pub enum Relation {
    #[sea_orm(
        belongs_to = "super::users::Entity",
        from = "Column::UserId",
        to = "super::users::Column::Id"
    )]
    User,
}

impl Related<super::users::Entity> for Entity {
    fn to() -> RelationDef { Relation::User.def() }
}

impl Related<super::walk_dogs::Entity> for Entity {
    fn to() -> RelationDef {
        super::walk_dogs::Relation::Dog.def().rev()
    }
}

impl ActiveModelBehavior for ActiveModel {}
```

- [ ] **Step 4: entities/walks.rsを作成**

```rust
// apps/api/src/entities/walks.rs
use sea_orm::entity::prelude::*;

#[derive(Clone, Debug, PartialEq, DeriveEntityModel)]
#[sea_orm(table_name = "walks")]
pub struct Model {
    #[sea_orm(primary_key, auto_increment = false)]
    pub id: Uuid,
    pub user_id: Uuid,
    pub status: String,
    pub distance_m: Option<i32>,
    pub duration_sec: Option<i32>,
    pub started_at: DateTimeWithTimeZone,
    pub ended_at: Option<DateTimeWithTimeZone>,
}

#[derive(Copy, Clone, Debug, EnumIter, DeriveRelation)]
pub enum Relation {
    #[sea_orm(
        belongs_to = "super::users::Entity",
        from = "Column::UserId",
        to = "super::users::Column::Id"
    )]
    User,
}

impl Related<super::users::Entity> for Entity {
    fn to() -> RelationDef { Relation::User.def() }
}

impl Related<super::dogs::Entity> for Entity {
    fn to() -> RelationDef {
        super::walk_dogs::Relation::Walk.def().rev()
    }
}

impl ActiveModelBehavior for ActiveModel {}
```

- [ ] **Step 5: entities/walk_dogs.rsを作成**

```rust
// apps/api/src/entities/walk_dogs.rs
use sea_orm::entity::prelude::*;

#[derive(Clone, Debug, PartialEq, DeriveEntityModel)]
#[sea_orm(table_name = "walk_dogs")]
pub struct Model {
    #[sea_orm(primary_key, auto_increment = false)]
    pub walk_id: Uuid,
    #[sea_orm(primary_key, auto_increment = false)]
    pub dog_id: Uuid,
}

#[derive(Copy, Clone, Debug, EnumIter, DeriveRelation)]
pub enum Relation {
    #[sea_orm(
        belongs_to = "super::walks::Entity",
        from = "Column::WalkId",
        to = "super::walks::Column::Id"
    )]
    Walk,
    #[sea_orm(
        belongs_to = "super::dogs::Entity",
        from = "Column::DogId",
        to = "super::dogs::Column::Id"
    )]
    Dog,
}

impl ActiveModelBehavior for ActiveModel {}
```

- [ ] **Step 6: entities/mod.rsを作成**

```rust
// apps/api/src/entities/mod.rs
pub mod dogs;
pub mod users;
pub mod walk_dogs;
pub mod walks;
```

- [ ] **Step 7: ビルド確認**

```bash
cd apps/api && cargo build 2>&1 | head -30
```

Expected: コンパイルエラーなし

- [ ] **Step 8: コミット**

```bash
git add apps/api/src/db/ apps/api/src/entities/
git commit -m "feat(api): add SeaORM entities and DB connection"
```

---

## Task 5: AWSクライアントとTEST_MODE認証

**Files:**
- Create: `apps/api/src/aws/mod.rs`
- Create: `apps/api/src/aws/client.rs`
- Create: `apps/api/src/auth/mod.rs`

- [ ] **Step 1: aws/client.rsを作成**

> **重要**: `aws_endpoint_url` を AWS クライアントに必ず渡す。渡さないとLocalStackに接続できない。

```rust
// apps/api/src/aws/client.rs
use aws_config::{BehaviorVersion, Region};
use aws_sdk_dynamodb::Client as DynamoClient;
use aws_sdk_s3::Client as S3Client;

pub async fn build_dynamo_client(region: &str, endpoint_url: Option<&str>) -> DynamoClient {
    let mut builder = aws_config::defaults(BehaviorVersion::latest())
        .region(Region::new(region.to_string()));
    if let Some(url) = endpoint_url {
        builder = builder.endpoint_url(url);
    }
    let config = builder.load().await;
    DynamoClient::new(&config)
}

pub async fn build_s3_client(region: &str, endpoint_url: Option<&str>) -> S3Client {
    let mut builder = aws_config::defaults(BehaviorVersion::latest())
        .region(Region::new(region.to_string()));
    if let Some(url) = endpoint_url {
        builder = builder.endpoint_url(url);
    }
    let config = builder.load().await;
    S3Client::new(&config)
}
```

- [ ] **Step 2: aws/mod.rsを作成**

```rust
// apps/api/src/aws/mod.rs
pub mod client;
```

- [ ] **Step 3: auth/mod.rsを作成 (TEST_MODE bypass付き)**

> **重要**: `TEST_MODE=true` のとき、JWT検証をスキップして固定の `cognito_sub` を返す。本番では `TEST_MODE` 環境変数を設定しないこと。

```rust
// apps/api/src/auth/mod.rs
use axum::{
    extract::Request,
    http::StatusCode,
    middleware::Next,
    response::Response,
};
use jsonwebtoken::{decode, decode_header, Algorithm, DecodingKey, Validation};
use serde::Deserialize;

/// Axumのリクエスト拡張に認証済みユーザー情報を付与する
#[derive(Clone, Debug)]
pub struct AuthUser {
    pub cognito_sub: String,
}

#[derive(Debug, Deserialize)]
struct CognitoClaims {
    sub: String,
    exp: usize,
}

/// JWT検証ミドルウェア
/// TEST_MODE=true のとき、JWT検証をスキップして固定のcognito_subを返す
pub async fn auth_middleware(
    mut request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    // TEST_MODE: JWT検証をスキップ
    if std::env::var("TEST_MODE").map(|v| v == "true" || v == "1").unwrap_or(false) {
        request.extensions_mut().insert(AuthUser {
            cognito_sub: "test-user-cognito-sub".to_string(),
        });
        return Ok(next.run(request).await);
    }

    let auth_header = request
        .headers()
        .get("Authorization")
        .and_then(|v| v.to_str().ok())
        .and_then(|v| v.strip_prefix("Bearer "));

    let token = match auth_header {
        Some(t) => t.to_string(),
        None => return Err(StatusCode::UNAUTHORIZED),
    };

    let cognito_sub = match verify_cognito_jwt(&token).await {
        Ok(sub) => sub,
        Err(_) => return Err(StatusCode::UNAUTHORIZED),
    };

    request.extensions_mut().insert(AuthUser { cognito_sub });
    Ok(next.run(request).await)
}

async fn verify_cognito_jwt(token: &str) -> Result<String, String> {
    let header = decode_header(token).map_err(|e| e.to_string())?;
    let kid = header.kid.ok_or("missing kid")?;

    let user_pool_id = std::env::var("COGNITO_USER_POOL_ID").map_err(|e| e.to_string())?;
    let region = std::env::var("COGNITO_REGION").unwrap_or_else(|_| "ap-northeast-1".to_string());

    // COGNITO_ENDPOINT_URL が設定されている場合は cognito-local を使う
    let jwks_url = if let Ok(endpoint) = std::env::var("COGNITO_ENDPOINT_URL") {
        format!("{}/{}/.well-known/jwks.json", endpoint, user_pool_id)
    } else {
        format!(
            "https://cognito-idp.{}.amazonaws.com/{}/.well-known/jwks.json",
            region, user_pool_id
        )
    };

    let jwks: serde_json::Value = reqwest::get(&jwks_url)
        .await
        .map_err(|e| e.to_string())?
        .json()
        .await
        .map_err(|e| e.to_string())?;

    let key = jwks["keys"]
        .as_array()
        .and_then(|keys| keys.iter().find(|k| k["kid"] == kid))
        .ok_or("key not found")?;

    let n = key["n"].as_str().ok_or("missing n")?;
    let e = key["e"].as_str().ok_or("missing e")?;
    let decoding_key = DecodingKey::from_rsa_components(n, e).map_err(|e| e.to_string())?;

    let mut validation = Validation::new(Algorithm::RS256);
    validation.set_issuer(&[format!(
        "https://cognito-idp.{}.amazonaws.com/{}",
        region, user_pool_id
    )]);

    let token_data = decode::<CognitoClaims>(token, &decoding_key, &validation)
        .map_err(|e| e.to_string())?;

    Ok(token_data.claims.sub)
}
```

- [ ] **Step 4: ビルド確認**

```bash
cd apps/api && cargo build 2>&1 | head -30
```

Expected: コンパイルエラーなし

- [ ] **Step 5: コミット**

```bash
git add apps/api/src/aws/ apps/api/src/auth/
git commit -m "feat(api): add AWS clients with endpoint override and TEST_MODE auth bypass"
```

---

## Task 6: GraphQLスキーマとmeクエリ

**Files:**
- Create: `apps/api/src/graphql/mod.rs`
- Create: `apps/api/src/graphql/context.rs`
- Create: `apps/api/src/graphql/types/mod.rs`
- Create: `apps/api/src/graphql/types/user.rs`
- Create: `apps/api/src/graphql/query/mod.rs`
- Create: `apps/api/src/graphql/query/user.rs`
- Create: `apps/api/src/services/user_service.rs`
- Test: `apps/api/tests/test_user.rs`

- [ ] **Step 1: tests/common/mod.rsを作成**

全テストが依存する `test_client()` ヘルパーを実装する。テスト用DBへのマイグレーションを実行し、Axumサーバーをバックグラウンドで起動して `reqwest::Client` を返す。

```rust
// apps/api/tests/common/mod.rs
use std::net::SocketAddr;
use tokio::net::TcpListener;

/// reqwest::Client は base_url をサポートしないため、
/// ベースURLを保持するラッパー構造体を使う。
pub struct TestClient {
    client: reqwest::Client,
    base_url: String,
}

impl TestClient {
    pub fn post(&self, path: &str) -> reqwest::RequestBuilder {
        self.client.post(format!("{}{}", self.base_url, path))
    }
    pub fn get(&self, path: &str) -> reqwest::RequestBuilder {
        self.client.get(format!("{}{}", self.base_url, path))
    }
}

/// テスト用サーバーを起動し TestClient を返す。
/// - TEST_MODE=true を設定 (JWT検証スキップ)
/// - テスト用PostgreSQLにマイグレーションを実行
/// - ランダムポートでAxumサーバーを起動
pub async fn test_client() -> TestClient {
    // TEST_MODE=true を設定 (JWT検証をスキップ)
    std::env::set_var("TEST_MODE", "true");

    dotenvy::dotenv().ok();
    let config = walking_dog_api::config::Config::from_env();

    // DBマイグレーション実行
    let db = walking_dog_api::db::connect(&config.database_url).await
        .expect("Failed to connect to test DB");
    migration::Migrator::up(&db, None).await
        .expect("Failed to run migrations");

    // AWSクライアントを構築
    let dynamo = walking_dog_api::aws::client::build_dynamo_client(
        &config.aws_region,
        config.aws_endpoint_url.as_deref(),
    ).await;
    let s3 = walking_dog_api::aws::client::build_s3_client(
        &config.aws_region,
        config.aws_endpoint_url.as_deref(),
    ).await;

    // GraphQLスキーマとAxumアプリを構築
    let app = walking_dog_api::build_app(db, dynamo, s3, config);

    // ランダムポートで起動
    let listener = TcpListener::bind("127.0.0.1:0").await.unwrap();
    let addr: SocketAddr = listener.local_addr().unwrap();

    tokio::spawn(async move {
        axum::serve(listener, app).await.unwrap();
    });

    TestClient {
        client: reqwest::Client::new(),
        base_url: format!("http://{}", addr),
    }
}
```

> **注意**: `walking_dog_api::build_app` はテスト用に `lib.rs` に公開する。`main.rs` はこれを呼ぶだけにする。全テストでは `common::TestClient` を使い、絶対URLで接続する。

- [ ] **Step 2: src/lib.rsを作成してテスト用にAPIを公開**

`AppContext` のライフタイム問題を避けるため、`AppState` を `Arc` で保持して `Schema` のデータとして格納する。リクエストごとに `cognito_sub` を差し込んでから `schema.execute()` を呼ぶ。

```rust
// apps/api/src/lib.rs
pub mod auth;
pub mod aws;
pub mod config;
pub mod db;
pub mod entities;
pub mod error;
pub mod graphql;
pub mod services;

use std::sync::Arc;
use axum::{Router, routing::get, middleware};
use async_graphql_axum::{GraphQLRequest, GraphQLResponse};
use sea_orm::DatabaseConnection;
use aws_sdk_dynamodb::Client as DynamoClient;
use aws_sdk_s3::Client as S3Client;
use crate::config::Config;
use crate::graphql::AppSchema;

/// アプリ全体で共有するリソース
pub struct AppState {
    pub db: DatabaseConnection,
    pub dynamo: DynamoClient,
    pub s3: S3Client,
    pub config: Config,
}

/// DB / AWS クライアント / Config を受け取り、Axum Router を返す。
/// GraphQL スキーマには Arc<AppState> を埋め込み、リクエストごとに cognito_sub を追加する。
pub fn build_app(
    db: DatabaseConnection,
    dynamo: DynamoClient,
    s3: S3Client,
    config: Config,
) -> Router {
    let state = Arc::new(AppState { db, dynamo, s3, config });

    let schema = async_graphql::Schema::build(
        graphql::QueryRoot::default(),
        graphql::MutationRoot::default(),
        async_graphql::EmptySubscription,
    )
    .data(state.clone())
    .finish();

    Router::new()
        .route("/health", get(|| async { "ok" }))
        .route("/graphql", axum::routing::post(graphql_handler))
        .layer(middleware::from_fn(auth::auth_middleware))
        .with_state(schema)
}

async fn graphql_handler(
    axum::extract::State(schema): axum::extract::State<AppSchema>,
    axum::Extension(auth_user): axum::Extension<auth::AuthUser>,
    req: GraphQLRequest,
) -> GraphQLResponse {
    let request = req.into_inner().data(auth_user.cognito_sub);
    schema.execute(request).await.into()
}
```

> **AppContext の変更**: リゾルバー内では `ctx.data::<Arc<AppState>>()` で db/dynamo/s3/config を取得し、`ctx.data::<String>()` で cognito_sub を取得する。または `graphql/context.rs` の `AppContext` 型を `Arc<AppState>` + `cognito_sub: String` を持つ struct に変更してハンドラーで組み立てても良い。実装時にどちらか一方に統一すること。

- [ ] **Step 3: test_user.rsのテストを書く**

```rust
// apps/api/tests/test_user.rs
mod common;

#[tokio::test]
async fn test_me_query() {
    let client = common::test_client().await;
    let res = client
        .post("/graphql")
        .header("Authorization", "Bearer test-token")
        .json(&serde_json::json!({
            "query": "{ me { id displayName dogs { id name } } }"
        }))
        .send()
        .await
        .unwrap();
    assert_eq!(res.status(), 200);
    let body: serde_json::Value = res.json().await.unwrap();
    assert!(body["data"]["me"]["id"].is_string());
    // 初期状態では dogs は空配列
    assert_eq!(body["data"]["me"]["dogs"], serde_json::json!([]));
}

#[tokio::test]
async fn test_update_profile_mutation() {
    let client = common::test_client().await;
    let res = client
        .post("/graphql")
        .header("Authorization", "Bearer test-token")
        .json(&serde_json::json!({
            "query": r#"mutation { updateProfile(input: { displayName: "Taro" }) { id displayName } }"#
        }))
        .send()
        .await
        .unwrap();
    assert_eq!(res.status(), 200);
    let body: serde_json::Value = res.json().await.unwrap();
    assert_eq!(body["data"]["updateProfile"]["displayName"], "Taro");
}
```

- [ ] **Step 2: テスト実行 (失敗を確認)**

```bash
cd apps/api
TEST_MODE=true cargo test test_me_query -- --nocapture 2>&1 | tail -10
```

Expected: FAIL (graphql module not yet implemented)

- [ ] **Step 3: graphql/context.rsを作成**

```rust
// apps/api/src/graphql/context.rs
use sea_orm::DatabaseConnection;
use aws_sdk_dynamodb::Client as DynamoClient;
use aws_sdk_s3::Client as S3Client;
use crate::config::Config;

pub struct AppContext {
    pub db: DatabaseConnection,
    pub dynamo: DynamoClient,
    pub s3: S3Client,
    pub config: Config,
    pub cognito_sub: String,
}
```

- [ ] **Step 4: User GraphQL型を作成**

> **重要**: `User` は `#[Object]` マクロを使う。`dogs` フィールドはリゾルバーで非同期取得するため `#[derive(SimpleObject)]` は使えない。

```rust
// apps/api/src/graphql/types/user.rs
use async_graphql::{Context, Object, Result, ID};
use crate::graphql::context::AppContext;
use crate::services::dog_service;

pub struct User {
    pub id: uuid::Uuid,
    pub display_name: Option<String>,
    pub avatar_url: Option<String>,
    pub created_at: chrono::DateTime<chrono::FixedOffset>,
}

#[Object]
impl User {
    async fn id(&self) -> ID { ID(self.id.to_string()) }
    async fn display_name(&self) -> Option<&str> { self.display_name.as_deref() }
    async fn avatar_url(&self) -> Option<&str> { self.avatar_url.as_deref() }
    async fn created_at(&self) -> String { self.created_at.to_rfc3339() }

    /// ユーザーが所有する犬一覧 (DBから非同期取得)
    async fn dogs(&self, ctx: &Context<'_>) -> Result<Vec<super::dog::Dog>> {
        let app_ctx = ctx.data::<AppContext>()?;
        let dogs = dog_service::get_dogs_by_user_id(&app_ctx.db, self.id).await?;
        Ok(dogs)
    }
}

impl From<crate::entities::users::Model> for User {
    fn from(m: crate::entities::users::Model) -> Self {
        Self {
            id: m.id,
            display_name: m.display_name,
            avatar_url: m.avatar_url,
            created_at: m.created_at.into(),
        }
    }
}
```

- [ ] **Step 5: Dog/BirthDate/WalkStats GraphQL型の骨格を作成**

> **重要**: `BirthDate` (出力型) と `BirthDateInput` (入力型) は必ず別の struct にする。`#[derive(SimpleObject, InputObject)]` を同一 struct に両方付けるとコンパイルエラーになる。

```rust
// apps/api/src/graphql/types/dog.rs
use async_graphql::{Context, InputObject, Object, Result, SimpleObject, ID};
use crate::graphql::context::AppContext;
use crate::services::walk_service;

// --- 出力型 ---

/// BirthDate: GraphQL出力型 (SimpleObject)
#[derive(SimpleObject, Clone, Debug)]
pub struct BirthDate {
    pub year: Option<i32>,
    pub month: Option<i32>,
    pub day: Option<i32>,
}

/// WalkStats: GraphQL出力型 (SimpleObject)
#[derive(SimpleObject, Clone, Debug)]
pub struct WalkStats {
    pub total_walks: i32,
    pub total_distance_m: i32,
    pub total_duration_sec: i32,
}

// --- 入力型 ---

/// BirthDateInput: GraphQL入力型 (InputObject) — BirthDateとは別の struct
#[derive(InputObject, Clone, Debug)]
pub struct BirthDateInput {
    pub year: Option<i32>,
    pub month: Option<i32>,
    pub day: Option<i32>,
}

impl BirthDateInput {
    pub fn to_json(&self) -> serde_json::Value {
        let mut map = serde_json::Map::new();
        if let Some(y) = self.year { map.insert("year".into(), y.into()); }
        if let Some(m) = self.month { map.insert("month".into(), m.into()); }
        if let Some(d) = self.day { map.insert("day".into(), d.into()); }
        serde_json::Value::Object(map)
    }
}

impl BirthDate {
    pub fn from_json(v: &serde_json::Value) -> Option<Self> {
        Some(Self {
            year: v.get("year").and_then(|v| v.as_i64()).map(|v| v as i32),
            month: v.get("month").and_then(|v| v.as_i64()).map(|v| v as i32),
            day: v.get("day").and_then(|v| v.as_i64()).map(|v| v as i32),
        })
    }
}

// --- Dog型 ---

pub struct Dog {
    pub id: uuid::Uuid,
    pub user_id: uuid::Uuid,
    pub name: String,
    pub breed: Option<String>,
    pub gender: Option<String>,
    pub birth_date: Option<BirthDate>,
    pub photo_url: Option<String>,
    pub created_at: chrono::DateTime<chrono::FixedOffset>,
}

#[Object]
impl Dog {
    async fn id(&self) -> ID { ID(self.id.to_string()) }
    async fn name(&self) -> &str { &self.name }
    async fn breed(&self) -> Option<&str> { self.breed.as_deref() }
    async fn gender(&self) -> Option<&str> { self.gender.as_deref() }
    async fn birth_date(&self) -> Option<&BirthDate> { self.birth_date.as_ref() }
    async fn photo_url(&self) -> Option<&str> { self.photo_url.as_deref() }
    async fn created_at(&self) -> String { self.created_at.to_rfc3339() }

    /// この犬の散歩履歴
    async fn walks(
        &self,
        ctx: &Context<'_>,
        limit: Option<i32>,
        offset: Option<i32>,
    ) -> Result<Vec<super::walk::Walk>> {
        let app_ctx = ctx.data::<AppContext>()?;
        let walks = walk_service::get_walks_by_dog_id(
            &app_ctx.db,
            self.id,
            limit.unwrap_or(20) as u64,
            offset.unwrap_or(0) as u64,
        ).await?;
        Ok(walks)
    }

    /// この犬の散歩統計
    async fn walk_stats(
        &self,
        ctx: &Context<'_>,
        period: StatsPeriod,
    ) -> Result<WalkStats> {
        let app_ctx = ctx.data::<AppContext>()?;
        walk_service::get_walk_stats(&app_ctx.db, self.id, period).await
    }
}

#[derive(async_graphql::Enum, Clone, Copy, PartialEq, Eq, Debug)]
pub enum StatsPeriod { Week, Month, Year, All }

impl From<crate::entities::dogs::Model> for Dog {
    fn from(m: crate::entities::dogs::Model) -> Self {
        let birth_date = m.birth_date.as_ref().and_then(BirthDate::from_json);
        Self {
            id: m.id,
            user_id: m.user_id,
            name: m.name,
            breed: m.breed,
            gender: m.gender,
            birth_date,
            photo_url: m.photo_url,
            created_at: m.created_at.into(),
        }
    }
}
```

- [ ] **Step 6: Walk GraphQL型の骨格を作成**

> **重要**: `Walk` は `#[Object]` マクロを使う。`dogs` フィールドはリゾルバーで非同期取得する。

```rust
// apps/api/src/graphql/types/walk.rs
use async_graphql::{Context, Object, Result, SimpleObject, ID};
use crate::graphql::context::AppContext;
use crate::services::dog_service;

#[derive(SimpleObject, Clone, Debug)]
pub struct WalkPoint {
    pub lat: f64,
    pub lng: f64,
    pub recorded_at: String,
}

pub struct Walk {
    pub id: uuid::Uuid,
    pub user_id: uuid::Uuid,
    pub status: String,
    pub distance_m: Option<i32>,
    pub duration_sec: Option<i32>,
    pub started_at: chrono::DateTime<chrono::FixedOffset>,
    pub ended_at: Option<chrono::DateTime<chrono::FixedOffset>>,
}

#[Object]
impl Walk {
    async fn id(&self) -> ID { ID(self.id.to_string()) }
    async fn status(&self) -> &str { &self.status }
    async fn distance_m(&self) -> Option<i32> { self.distance_m }
    async fn duration_sec(&self) -> Option<i32> { self.duration_sec }
    async fn started_at(&self) -> String { self.started_at.to_rfc3339() }
    async fn ended_at(&self) -> Option<String> { self.ended_at.map(|t| t.to_rfc3339()) }

    /// この散歩に参加した犬一覧 (DBから非同期取得)
    async fn dogs(&self, ctx: &Context<'_>) -> Result<Vec<super::dog::Dog>> {
        let app_ctx = ctx.data::<AppContext>()?;
        let dogs = dog_service::get_dogs_by_walk_id(&app_ctx.db, self.id).await?;
        Ok(dogs)
    }

    /// WalkPointsはDynamoDBから取得 (Task 10で実装)
    async fn points(&self, ctx: &Context<'_>) -> Result<Vec<WalkPoint>> {
        let app_ctx = ctx.data::<AppContext>()?;
        crate::services::walk_points_service::get_walk_points(
            &app_ctx.dynamo,
            &app_ctx.config.dynamodb_table_walk_points,
            self.id,
        ).await
    }
}

impl From<crate::entities::walks::Model> for Walk {
    fn from(m: crate::entities::walks::Model) -> Self {
        Self {
            id: m.id,
            user_id: m.user_id,
            status: m.status,
            distance_m: m.distance_m,
            duration_sec: m.duration_sec,
            started_at: m.started_at.into(),
            ended_at: m.ended_at.map(|t| t.into()),
        }
    }
}
```

- [ ] **Step 7: user_service.rsを作成**

```rust
// apps/api/src/services/user_service.rs
use sea_orm::{ActiveModelTrait, ColumnTrait, EntityTrait, QueryFilter, Set};
use uuid::Uuid;
use crate::entities::users::{self, ActiveModel, Entity as UserEntity};
use crate::error::AppError;
use crate::graphql::types::user::User;

pub async fn get_or_create_user(
    db: &sea_orm::DatabaseConnection,
    cognito_sub: &str,
) -> Result<User, AppError> {
    if let Some(model) = UserEntity::find()
        .filter(users::Column::CognitoSub.eq(cognito_sub))
        .one(db)
        .await?
    {
        return Ok(model.into());
    }

    let model = ActiveModel {
        id: Set(Uuid::new_v4()),
        cognito_sub: Set(cognito_sub.to_string()),
        ..Default::default()
    }
    .insert(db)
    .await?;

    Ok(model.into())
}

pub async fn update_profile(
    db: &sea_orm::DatabaseConnection,
    cognito_sub: &str,
    display_name: Option<String>,
) -> Result<User, AppError> {
    let model = UserEntity::find()
        .filter(users::Column::CognitoSub.eq(cognito_sub))
        .one(db)
        .await?
        .ok_or_else(|| AppError::NotFound("User not found".to_string()))?;

    let mut active: ActiveModel = model.into();
    if let Some(name) = display_name {
        active.display_name = Set(Some(name));
    }
    let updated = active.update(db).await?;
    Ok(updated.into())
}
```

- [ ] **Step 8: query/user.rs, query/mod.rs, mutation/user.rsの骨格を作成**

```rust
// apps/api/src/graphql/query/user.rs
use async_graphql::{Context, Object, Result};
use crate::graphql::{context::AppContext, types::user::User};
use crate::services::user_service;

pub struct UserQuery;

#[Object]
impl UserQuery {
    async fn me(&self, ctx: &Context<'_>) -> Result<User> {
        let app_ctx = ctx.data::<AppContext>()?;
        Ok(user_service::get_or_create_user(&app_ctx.db, &app_ctx.cognito_sub).await?)
    }
}
```

```rust
// apps/api/src/graphql/mutation/user.rs
use async_graphql::{Context, InputObject, Object, Result};
use crate::graphql::{context::AppContext, types::user::User};
use crate::services::user_service;

#[derive(InputObject)]
pub struct UpdateProfileInput {
    pub display_name: Option<String>,
}

pub struct UserMutation;

#[Object]
impl UserMutation {
    async fn update_profile(
        &self,
        ctx: &Context<'_>,
        input: UpdateProfileInput,
    ) -> Result<User> {
        let app_ctx = ctx.data::<AppContext>()?;
        Ok(user_service::update_profile(&app_ctx.db, &app_ctx.cognito_sub, input.display_name).await?)
    }
}
```

- [ ] **Step 9: graphql/mod.rsとmain.rsを更新してGraphQL endpointを追加**

```rust
// apps/api/src/graphql/mod.rs
use async_graphql::{EmptySubscription, MergedObject, Schema};
use query::{user::UserQuery, dog::DogQuery, walk::WalkQuery};
use mutation::{user::UserMutation, dog::DogMutation, walk::WalkMutation};

pub mod context;
pub mod mutation;
pub mod query;
pub mod types;

#[derive(MergedObject, Default)]
pub struct QueryRoot(UserQuery, DogQuery, WalkQuery);

#[derive(MergedObject, Default)]
pub struct MutationRoot(UserMutation, DogMutation, WalkMutation);

pub type AppSchema = Schema<QueryRoot, MutationRoot, EmptySubscription>;
```

- [ ] **Step 10: テスト実行**

```bash
cd apps/api
TEST_MODE=true cargo test test_me_query test_update_profile_mutation -- --nocapture
```

Expected: 全テスト PASS

- [ ] **Step 11: コミット**

```bash
git add apps/api/src/graphql/ apps/api/src/services/user_service.rs apps/api/tests/test_user.rs
git commit -m "feat(api): add GraphQL schema with me query and User.dogs resolver"
```

---

## Task 7: 犬CRUD

**Files:**
- Create: `apps/api/src/services/dog_service.rs`
- Create: `apps/api/src/graphql/query/dog.rs`
- Create: `apps/api/src/graphql/mutation/dog.rs`
- Test: `apps/api/tests/test_dog.rs`

- [ ] **Step 1: test_dog.rsのテストを書く**

```rust
// apps/api/tests/test_dog.rs
mod common;

#[tokio::test]
async fn test_create_dog() {
    let client = common::test_client().await;
    let res = client
        .post("/graphql")
        .header("Authorization", "Bearer test-token")
        .json(&serde_json::json!({
            "query": r#"mutation {
                createDog(input: {
                    name: "Pochi",
                    breed: "Shiba",
                    gender: "male",
                    birthDate: { year: 2020, month: 4 }
                }) { id name breed gender birthDate { year month day } }
            }"#
        }))
        .send()
        .await
        .unwrap();
    assert_eq!(res.status(), 200);
    let body: serde_json::Value = res.json().await.unwrap();
    assert_eq!(body["data"]["createDog"]["name"], "Pochi");
    assert_eq!(body["data"]["createDog"]["birthDate"]["year"], 2020);
    assert_eq!(body["data"]["createDog"]["birthDate"]["month"], 4);
    assert!(body["data"]["createDog"]["birthDate"]["day"].is_null());
}

#[tokio::test]
async fn test_update_dog() {
    let client = common::test_client().await;
    // まず犬を作成
    let create_res = client
        .post("/graphql")
        .header("Authorization", "Bearer test-token")
        .json(&serde_json::json!({
            "query": r#"mutation { createDog(input: { name: "Hachi" }) { id } }"#
        }))
        .send().await.unwrap();
    let create_body: serde_json::Value = create_res.json().await.unwrap();
    let dog_id = create_body["data"]["createDog"]["id"].as_str().unwrap();

    // 更新
    let res = client
        .post("/graphql")
        .header("Authorization", "Bearer test-token")
        .json(&serde_json::json!({
            "query": format!(r#"mutation {{ updateDog(id: "{}", input: {{ name: "Hachi Updated" }}) {{ id name }} }}"#, dog_id)
        }))
        .send().await.unwrap();
    let body: serde_json::Value = res.json().await.unwrap();
    assert_eq!(body["data"]["updateDog"]["name"], "Hachi Updated");
}

#[tokio::test]
async fn test_delete_dog() {
    let client = common::test_client().await;
    let create_res = client
        .post("/graphql")
        .header("Authorization", "Bearer test-token")
        .json(&serde_json::json!({
            "query": r#"mutation { createDog(input: { name: "DeleteMe" }) { id } }"#
        }))
        .send().await.unwrap();
    let create_body: serde_json::Value = create_res.json().await.unwrap();
    let dog_id = create_body["data"]["createDog"]["id"].as_str().unwrap();

    let res = client
        .post("/graphql")
        .header("Authorization", "Bearer test-token")
        .json(&serde_json::json!({
            "query": format!(r#"mutation {{ deleteDog(id: "{}") }}"#, dog_id)
        }))
        .send().await.unwrap();
    let body: serde_json::Value = res.json().await.unwrap();
    assert_eq!(body["data"]["deleteDog"], true);
}
```

- [ ] **Step 2: テスト実行 (失敗を確認)**

```bash
cd apps/api
TEST_MODE=true cargo test test_create_dog -- --nocapture 2>&1 | tail -5
```

Expected: FAIL

- [ ] **Step 3: dog_service.rsを作成 (完全実装)**

```rust
// apps/api/src/services/dog_service.rs
use sea_orm::{
    ActiveModelTrait, ColumnTrait, EntityTrait, QueryFilter, Set, TransactionTrait,
};
use uuid::Uuid;
use crate::entities::{
    dogs::{self, ActiveModel, Entity as DogEntity},
    walk_dogs::{self, Entity as WalkDogEntity},
    walks::{self, Entity as WalkEntity},
};
use crate::error::AppError;
use crate::graphql::types::dog::Dog;

pub async fn create_dog(
    db: &sea_orm::DatabaseConnection,
    user_id: Uuid,
    name: String,
    breed: Option<String>,
    gender: Option<String>,
    birth_date: Option<serde_json::Value>,
) -> Result<Dog, AppError> {
    let model = ActiveModel {
        id: Set(Uuid::new_v4()),
        user_id: Set(user_id),
        name: Set(name),
        breed: Set(breed),
        gender: Set(gender),
        birth_date: Set(birth_date),
        ..Default::default()
    }
    .insert(db)
    .await?;
    Ok(model.into())
}

pub async fn update_dog(
    db: &sea_orm::DatabaseConnection,
    dog_id: Uuid,
    user_id: Uuid,
    name: Option<String>,
    breed: Option<String>,
    gender: Option<String>,
    birth_date: Option<serde_json::Value>,
) -> Result<Dog, AppError> {
    let model = DogEntity::find_by_id(dog_id)
        .filter(dogs::Column::UserId.eq(user_id))
        .one(db)
        .await?
        .ok_or_else(|| AppError::NotFound(format!("Dog {} not found", dog_id)))?;

    let mut active: ActiveModel = model.into();
    if let Some(n) = name { active.name = Set(n); }
    if let Some(b) = breed { active.breed = Set(Some(b)); }
    if let Some(g) = gender { active.gender = Set(Some(g)); }
    if let Some(bd) = birth_date { active.birth_date = Set(Some(bd)); }

    let updated = active.update(db).await?;
    Ok(updated.into())
}

/// 犬を削除する。
/// walk_dogsのON DELETE CASCADEで関連レコードが自動削除される。
/// 全参加犬が削除されたwalksをトランザクション内で削除する。
pub async fn delete_dog(
    db: &sea_orm::DatabaseConnection,
    dog_id: Uuid,
    user_id: Uuid,
) -> Result<bool, AppError> {
    let txn = db.begin().await?;

    let dog = DogEntity::find_by_id(dog_id)
        .filter(dogs::Column::UserId.eq(user_id))
        .one(&txn)
        .await?
        .ok_or_else(|| AppError::NotFound(format!("Dog {} not found", dog_id)))?;

    // この犬が参加しているwalk_idを取得
    let walk_dog_records = WalkDogEntity::find()
        .filter(walk_dogs::Column::DogId.eq(dog_id))
        .all(&txn)
        .await?;

    let walk_ids: Vec<Uuid> = walk_dog_records.iter().map(|wd| wd.walk_id).collect();

    // 犬を削除 (ON DELETE CASCADE で walk_dogs も削除)
    DogEntity::delete_by_id(dog.id).exec(&txn).await?;

    // 全参加犬が削除されたwalksを検出して削除
    for walk_id in walk_ids {
        let remaining = WalkDogEntity::find()
            .filter(walk_dogs::Column::WalkId.eq(walk_id))
            .count(&txn)
            .await?;
        if remaining == 0 {
            WalkEntity::delete_by_id(walk_id).exec(&txn).await?;
        }
    }

    txn.commit().await?;
    Ok(true)
}

pub async fn get_dog_by_id(
    db: &sea_orm::DatabaseConnection,
    dog_id: Uuid,
) -> Result<Option<Dog>, AppError> {
    let model = DogEntity::find_by_id(dog_id).one(db).await?;
    Ok(model.map(Dog::from))
}

pub async fn get_dogs_by_user_id(
    db: &sea_orm::DatabaseConnection,
    user_id: Uuid,
) -> Result<Vec<Dog>, AppError> {
    let models = DogEntity::find()
        .filter(dogs::Column::UserId.eq(user_id))
        .all(db)
        .await?;
    Ok(models.into_iter().map(Dog::from).collect())
}

pub async fn get_dogs_by_walk_id(
    db: &sea_orm::DatabaseConnection,
    walk_id: Uuid,
) -> Result<Vec<Dog>, AppError> {
    use crate::entities::dogs::Entity as DogEntity;
    use crate::entities::walk_dogs::Entity as WalkDogEntity;
    use sea_orm::QuerySelect;

    let dog_ids: Vec<Uuid> = WalkDogEntity::find()
        .filter(walk_dogs::Column::WalkId.eq(walk_id))
        .select_only()
        .column(walk_dogs::Column::DogId)
        .into_tuple()
        .all(db)
        .await?;

    let models = DogEntity::find()
        .filter(dogs::Column::Id.is_in(dog_ids))
        .all(db)
        .await?;

    Ok(models.into_iter().map(Dog::from).collect())
}
```

- [ ] **Step 4: query/dog.rsを作成**

```rust
// apps/api/src/graphql/query/dog.rs
use async_graphql::{Context, Object, Result, ID};
use crate::graphql::{context::AppContext, types::dog::Dog};
use crate::services::dog_service;

pub struct DogQuery;

#[Object]
impl DogQuery {
    async fn dog(&self, ctx: &Context<'_>, id: ID) -> Result<Option<Dog>> {
        let app_ctx = ctx.data::<AppContext>()?;
        let dog_id = uuid::Uuid::parse_str(&id).map_err(|e| async_graphql::Error::new(e.to_string()))?;
        Ok(dog_service::get_dog_by_id(&app_ctx.db, dog_id).await?)
    }
}
```

- [ ] **Step 5: mutation/dog.rsを作成**

```rust
// apps/api/src/graphql/mutation/dog.rs
use async_graphql::{Context, InputObject, Object, Result, ID};
use crate::graphql::{context::AppContext, types::dog::{BirthDateInput, Dog}};
use crate::services::{dog_service, user_service};

#[derive(InputObject)]
pub struct CreateDogInput {
    pub name: String,
    pub breed: Option<String>,
    pub gender: Option<String>,
    pub birth_date: Option<BirthDateInput>,
}

#[derive(InputObject)]
pub struct UpdateDogInput {
    pub name: Option<String>,
    pub breed: Option<String>,
    pub gender: Option<String>,
    pub birth_date: Option<BirthDateInput>,
}

pub struct DogMutation;

#[Object]
impl DogMutation {
    async fn create_dog(&self, ctx: &Context<'_>, input: CreateDogInput) -> Result<Dog> {
        let app_ctx = ctx.data::<AppContext>()?;
        let user = user_service::get_or_create_user(&app_ctx.db, &app_ctx.cognito_sub).await?;
        let birth_date_json = input.birth_date.as_ref().map(|bd| bd.to_json());
        Ok(dog_service::create_dog(
            &app_ctx.db,
            user.id,
            input.name,
            input.breed,
            input.gender,
            birth_date_json,
        ).await?)
    }

    async fn update_dog(&self, ctx: &Context<'_>, id: ID, input: UpdateDogInput) -> Result<Dog> {
        let app_ctx = ctx.data::<AppContext>()?;
        let user = user_service::get_or_create_user(&app_ctx.db, &app_ctx.cognito_sub).await?;
        let dog_id = uuid::Uuid::parse_str(&id).map_err(|e| async_graphql::Error::new(e.to_string()))?;
        let birth_date_json = input.birth_date.as_ref().map(|bd| bd.to_json());
        Ok(dog_service::update_dog(
            &app_ctx.db,
            dog_id,
            user.id,
            input.name,
            input.breed,
            input.gender,
            birth_date_json,
        ).await?)
    }

    async fn delete_dog(&self, ctx: &Context<'_>, id: ID) -> Result<bool> {
        let app_ctx = ctx.data::<AppContext>()?;
        let user = user_service::get_or_create_user(&app_ctx.db, &app_ctx.cognito_sub).await?;
        let dog_id = uuid::Uuid::parse_str(&id).map_err(|e| async_graphql::Error::new(e.to_string()))?;
        Ok(dog_service::delete_dog(&app_ctx.db, dog_id, user.id).await?)
    }
}
```

- [ ] **Step 6: テスト実行**

```bash
cd apps/api
TEST_MODE=true cargo test test_create_dog test_update_dog test_delete_dog -- --nocapture
```

Expected: 全テスト PASS

- [ ] **Step 7: コミット**

```bash
git add apps/api/src/services/dog_service.rs apps/api/src/graphql/query/dog.rs apps/api/src/graphql/mutation/dog.rs apps/api/tests/test_dog.rs
git commit -m "feat(api): add Dog CRUD with cascade delete and BirthDate support"
```

---

## Task 8: S3プレサインドURL (犬の写真アップロード)

**Files:**
- Create: `apps/api/src/services/s3_service.rs` (または `aws/s3.rs` に統合)
- Modify: `apps/api/src/graphql/mutation/dog.rs`

- [ ] **Step 1: test_dog.rsに`test_generate_dog_photo_upload_url`テストを追加**

```rust
// apps/api/tests/test_dog.rs に追加
#[tokio::test]
async fn test_generate_dog_photo_upload_url() {
    let client = common::test_client().await;
    // 犬を作成
    let create_res = client
        .post("/graphql")
        .header("Authorization", "Bearer test-token")
        .json(&serde_json::json!({
            "query": r#"mutation { createDog(input: { name: "PhotoDog" }) { id } }"#
        }))
        .send().await.unwrap();
    let create_body: serde_json::Value = create_res.json().await.unwrap();
    let dog_id = create_body["data"]["createDog"]["id"].as_str().unwrap();

    // プレサインドURL取得
    let res = client
        .post("/graphql")
        .header("Authorization", "Bearer test-token")
        .json(&serde_json::json!({
            "query": format!(
                r#"mutation {{ generateDogPhotoUploadUrl(dogId: "{}") {{ url key expiresAt }} }}"#,
                dog_id
            )
        }))
        .send().await.unwrap();
    assert_eq!(res.status(), 200);
    let body: serde_json::Value = res.json().await.unwrap();
    let url = body["data"]["generateDogPhotoUploadUrl"]["url"].as_str().unwrap();
    assert!(url.starts_with("http"), "URL should be an HTTP URL, got: {}", url);
    assert!(body["data"]["generateDogPhotoUploadUrl"]["key"].is_string());
    assert!(body["data"]["generateDogPhotoUploadUrl"]["expiresAt"].is_string());
}
```

- [ ] **Step 2: テスト実行 (失敗を確認)**

```bash
cd apps/api
TEST_MODE=true cargo test test_generate_dog_photo_upload_url -- --nocapture 2>&1 | tail -5
```

Expected: FAIL (s3_service not yet implemented)

- [ ] **Step 3: s3_service.rsを作成**

> **注意**: `aws-sdk-s3` v1 では `presigning()` メソッドが直接利用できる。別クレートは不要。

```rust
// apps/api/src/services/s3_service.rs
use aws_sdk_s3::{
    Client as S3Client,
    presigning::PresigningConfig,
};
use std::time::Duration;
use uuid::Uuid;

pub struct PresignedUrl {
    pub url: String,
    pub key: String,
    pub expires_at: chrono::DateTime<chrono::Utc>,
}

pub async fn generate_dog_photo_upload_url(
    s3: &S3Client,
    bucket: &str,
    dog_id: Uuid,
) -> Result<PresignedUrl, crate::error::AppError> {
    let key = format!("dogs/{}/{}.jpg", dog_id, Uuid::new_v4());
    let expires_in = Duration::from_secs(3600);

    let presigned = s3
        .put_object()
        .bucket(bucket)
        .key(&key)
        .content_type("image/jpeg")
        .presigned(
            PresigningConfig::expires_in(expires_in)
                .map_err(|e| crate::error::AppError::Internal(e.to_string()))?,
        )
        .await
        .map_err(|e| crate::error::AppError::Internal(e.to_string()))?;

    let expires_at = chrono::Utc::now() + chrono::Duration::seconds(3600);
    Ok(PresignedUrl {
        url: presigned.uri().to_string(),
        key,
        expires_at,
    })
}
```

- [ ] **Step 4: mutation/dog.rsにgenerateDogPhotoUploadUrlを追加**

`DogMutation` の `#[Object]` impl に以下を追加:

```rust
async fn generate_dog_photo_upload_url(
    &self,
    ctx: &Context<'_>,
    dog_id: ID,
) -> Result<PresignedUrlOutput> {
    let app_ctx = ctx.data::<AppContext>()?;
    let dog_uuid = uuid::Uuid::parse_str(&dog_id)
        .map_err(|e| async_graphql::Error::new(e.to_string()))?;
    let result = crate::services::s3_service::generate_dog_photo_upload_url(
        &app_ctx.s3,
        &app_ctx.config.s3_bucket_dog_photos,
        dog_uuid,
    ).await?;
    Ok(PresignedUrlOutput {
        url: result.url,
        key: result.key,
        expires_at: result.expires_at.to_rfc3339(),
    })
}
```

```rust
// PresignedUrlOutput型 (mutation/dog.rsに追加)
#[derive(async_graphql::SimpleObject)]
pub struct PresignedUrlOutput {
    pub url: String,
    pub key: String,
    pub expires_at: String,
}
```

- [ ] **Step 5: テスト実行**

```bash
cd apps/api
TEST_MODE=true cargo test test_generate_dog_photo_upload_url -- --nocapture
```

Expected: PASS

- [ ] **Step 6: コミット**

```bash
git add apps/api/src/services/s3_service.rs apps/api/src/graphql/mutation/dog.rs apps/api/tests/test_dog.rs
git commit -m "feat(api): add S3 presigned URL generation for dog photos"
```

---

## Task 9: 散歩CRUD

**Files:**
- Create: `apps/api/src/services/walk_service.rs`
- Create: `apps/api/src/graphql/query/walk.rs`
- Create: `apps/api/src/graphql/mutation/walk.rs`
- Test: `apps/api/tests/test_walk.rs`

- [ ] **Step 1: test_walk.rsのテストを書く**

```rust
// apps/api/tests/test_walk.rs
mod common;

async fn create_test_dog(client: &reqwest::Client) -> String {
    let res = client
        .post("/graphql")
        .header("Authorization", "Bearer test-token")
        .json(&serde_json::json!({
            "query": r#"mutation { createDog(input: { name: "WalkDog" }) { id } }"#
        }))
        .send().await.unwrap();
    let body: serde_json::Value = res.json().await.unwrap();
    body["data"]["createDog"]["id"].as_str().unwrap().to_string()
}

#[tokio::test]
async fn test_start_walk() {
    let client = common::test_client().await;
    let dog_id = create_test_dog(&client).await;

    let res = client
        .post("/graphql")
        .header("Authorization", "Bearer test-token")
        .json(&serde_json::json!({
            "query": format!(r#"mutation {{ startWalk(dogIds: ["{}"]) {{ id status dogs {{ id name }} }} }}"#, dog_id)
        }))
        .send().await.unwrap();
    assert_eq!(res.status(), 200);
    let body: serde_json::Value = res.json().await.unwrap();
    assert_eq!(body["data"]["startWalk"]["status"], "ACTIVE");
    assert_eq!(body["data"]["startWalk"]["dogs"][0]["id"], dog_id);
}

#[tokio::test]
async fn test_start_walk_empty_dogs_returns_error() {
    let client = common::test_client().await;
    let res = client
        .post("/graphql")
        .header("Authorization", "Bearer test-token")
        .json(&serde_json::json!({
            "query": r#"mutation { startWalk(dogIds: []) { id } }"#
        }))
        .send().await.unwrap();
    let body: serde_json::Value = res.json().await.unwrap();
    assert!(body["errors"].is_array());
    let err_msg = body["errors"][0]["message"].as_str().unwrap();
    assert!(err_msg.contains("dogIds") || err_msg.to_lowercase().contains("empty"));
}

#[tokio::test]
async fn test_finish_walk() {
    let client = common::test_client().await;
    let dog_id = create_test_dog(&client).await;

    // 散歩開始
    let start_res = client
        .post("/graphql")
        .header("Authorization", "Bearer test-token")
        .json(&serde_json::json!({
            "query": format!(r#"mutation {{ startWalk(dogIds: ["{}"]) {{ id }} }}"#, dog_id)
        }))
        .send().await.unwrap();
    let start_body: serde_json::Value = start_res.json().await.unwrap();
    let walk_id = start_body["data"]["startWalk"]["id"].as_str().unwrap();

    // 散歩終了
    let res = client
        .post("/graphql")
        .header("Authorization", "Bearer test-token")
        .json(&serde_json::json!({
            "query": format!(r#"mutation {{ finishWalk(walkId: "{}") {{ id status endedAt }} }}"#, walk_id)
        }))
        .send().await.unwrap();
    let body: serde_json::Value = res.json().await.unwrap();
    assert_eq!(body["data"]["finishWalk"]["status"], "FINISHED");
    assert!(body["data"]["finishWalk"]["endedAt"].is_string());
}

#[tokio::test]
async fn test_my_walks_query() {
    let client = common::test_client().await;
    let dog_id = create_test_dog(&client).await;

    // 散歩を2件作成
    for _ in 0..2 {
        client.post("/graphql")
            .header("Authorization", "Bearer test-token")
            .json(&serde_json::json!({
                "query": format!(r#"mutation {{ startWalk(dogIds: ["{}"]) {{ id }} }}"#, dog_id)
            }))
            .send().await.unwrap();
    }

    let res = client
        .post("/graphql")
        .header("Authorization", "Bearer test-token")
        .json(&serde_json::json!({
            "query": r#"{ myWalks { id status dogs { id name } } }"#
        }))
        .send().await.unwrap();
    assert_eq!(res.status(), 200);
    let body: serde_json::Value = res.json().await.unwrap();
    let walks = body["data"]["myWalks"].as_array().unwrap();
    assert!(walks.len() >= 2);
    // 各散歩に dogs が含まれることを確認
    assert!(walks[0]["dogs"].is_array());
}
```

- [ ] **Step 2: テスト実行 (失敗を確認)**

```bash
cd apps/api
TEST_MODE=true cargo test test_start_walk -- --nocapture 2>&1 | tail -5
```

Expected: FAIL

- [ ] **Step 3: walk_service.rsを作成**

```rust
// apps/api/src/services/walk_service.rs
use sea_orm::{
    ActiveModelTrait, ColumnTrait, EntityTrait, QueryFilter, QueryOrder, QuerySelect, Set,
};
use uuid::Uuid;
use chrono::Utc;
use crate::entities::{
    walks::{self, ActiveModel, Entity as WalkEntity},
    walk_dogs::{self, ActiveModel as WalkDogActiveModel, Entity as WalkDogEntity},
};
use crate::error::AppError;
use crate::graphql::types::{
    dog::{StatsPeriod, WalkStats},
    walk::Walk,
};

pub async fn start_walk(
    db: &sea_orm::DatabaseConnection,
    user_id: Uuid,
    dog_ids: Vec<Uuid>,
) -> Result<Walk, AppError> {
    if dog_ids.is_empty() {
        return Err(AppError::BadRequest("dogIds must not be empty".to_string()));
    }

    let walk = ActiveModel {
        id: Set(Uuid::new_v4()),
        user_id: Set(user_id),
        status: Set("active".to_string()),
        started_at: Set(Utc::now().into()),
        ..Default::default()
    }
    .insert(db)
    .await?;

    for dog_id in dog_ids {
        WalkDogActiveModel {
            walk_id: Set(walk.id),
            dog_id: Set(dog_id),
        }
        .insert(db)
        .await?;
    }

    Ok(walk.into())
}

pub async fn finish_walk(
    db: &sea_orm::DatabaseConnection,
    walk_id: Uuid,
    user_id: Uuid,
) -> Result<Walk, AppError> {
    let walk = WalkEntity::find_by_id(walk_id)
        .filter(walks::Column::UserId.eq(user_id))
        .one(db)
        .await?
        .ok_or_else(|| AppError::NotFound(format!("Walk {} not found", walk_id)))?;

    let started_at: chrono::DateTime<chrono::Utc> = walk.started_at.into();
    let ended_at = Utc::now();
    let duration_sec = (ended_at - started_at).num_seconds() as i32;

    let mut active: walks::ActiveModel = walk.into();
    active.status = Set("finished".to_string());
    active.ended_at = Set(Some(ended_at.into()));
    active.duration_sec = Set(Some(duration_sec));

    let updated = active.update(db).await?;
    Ok(updated.into())
}

pub async fn get_walk_by_id(
    db: &sea_orm::DatabaseConnection,
    walk_id: Uuid,
) -> Result<Option<Walk>, AppError> {
    let model = WalkEntity::find_by_id(walk_id).one(db).await?;
    Ok(model.map(Walk::from))
}

pub async fn get_my_walks(
    db: &sea_orm::DatabaseConnection,
    user_id: Uuid,
    limit: u64,
    offset: u64,
) -> Result<Vec<Walk>, AppError> {
    let models = WalkEntity::find()
        .filter(walks::Column::UserId.eq(user_id))
        .order_by_desc(walks::Column::StartedAt)
        .limit(limit)
        .offset(offset)
        .all(db)
        .await?;
    Ok(models.into_iter().map(Walk::from).collect())
}

pub async fn get_walks_by_dog_id(
    db: &sea_orm::DatabaseConnection,
    dog_id: Uuid,
    limit: u64,
    offset: u64,
) -> Result<Vec<Walk>, AppError> {
    let walk_ids: Vec<Uuid> = WalkDogEntity::find()
        .filter(walk_dogs::Column::DogId.eq(dog_id))
        .select_only()
        .column(walk_dogs::Column::WalkId)
        .into_tuple()
        .all(db)
        .await?;

    let models = WalkEntity::find()
        .filter(walks::Column::Id.is_in(walk_ids))
        .order_by_desc(walks::Column::StartedAt)
        .limit(limit)
        .offset(offset)
        .all(db)
        .await?;

    Ok(models.into_iter().map(Walk::from).collect())
}

pub async fn get_walk_stats(
    db: &sea_orm::DatabaseConnection,
    dog_id: Uuid,
    period: StatsPeriod,
) -> Result<WalkStats, AppError> {
    use sea_orm::prelude::*;

    let now = Utc::now();
    let since: Option<chrono::DateTime<chrono::Utc>> = match period {
        StatsPeriod::Week => Some(now - chrono::Duration::weeks(1)),
        StatsPeriod::Month => Some(now - chrono::Duration::days(30)),
        StatsPeriod::Year => Some(now - chrono::Duration::days(365)),
        StatsPeriod::All => None,
    };

    let walk_ids: Vec<Uuid> = WalkDogEntity::find()
        .filter(walk_dogs::Column::DogId.eq(dog_id))
        .select_only()
        .column(walk_dogs::Column::WalkId)
        .into_tuple()
        .all(db)
        .await?;

    let mut query = WalkEntity::find()
        .filter(walks::Column::Id.is_in(walk_ids))
        .filter(walks::Column::Status.eq("finished"));

    if let Some(s) = since {
        query = query.filter(walks::Column::StartedAt.gte(s.into()));
    }

    let walks = query.all(db).await?;

    let total_walks = walks.len() as i32;
    let total_distance_m = walks.iter().filter_map(|w| w.distance_m).sum();
    let total_duration_sec = walks.iter().filter_map(|w| w.duration_sec).sum();

    Ok(WalkStats { total_walks, total_distance_m, total_duration_sec })
}
```

- [ ] **Step 4: query/walk.rsとmutation/walk.rsを作成**

```rust
// apps/api/src/graphql/query/walk.rs
use async_graphql::{Context, Object, Result, ID};
use crate::graphql::{context::AppContext, types::walk::Walk};
use crate::services::{user_service, walk_service};

pub struct WalkQuery;

#[Object]
impl WalkQuery {
    async fn walk(&self, ctx: &Context<'_>, id: ID) -> Result<Option<Walk>> {
        let app_ctx = ctx.data::<AppContext>()?;
        let walk_id = uuid::Uuid::parse_str(&id)
            .map_err(|e| async_graphql::Error::new(e.to_string()))?;
        Ok(walk_service::get_walk_by_id(&app_ctx.db, walk_id).await?)
    }

    async fn my_walks(
        &self,
        ctx: &Context<'_>,
        limit: Option<i32>,
        offset: Option<i32>,
    ) -> Result<Vec<Walk>> {
        let app_ctx = ctx.data::<AppContext>()?;
        let user = user_service::get_or_create_user(&app_ctx.db, &app_ctx.cognito_sub).await?;
        Ok(walk_service::get_my_walks(
            &app_ctx.db,
            user.id,
            limit.unwrap_or(20) as u64,
            offset.unwrap_or(0) as u64,
        ).await?)
    }
}
```

```rust
// apps/api/src/graphql/mutation/walk.rs
use async_graphql::{Context, Object, Result, ID};
use crate::graphql::{context::AppContext, types::walk::Walk};
use crate::services::{user_service, walk_service};

pub struct WalkMutation;

#[Object]
impl WalkMutation {
    async fn start_walk(&self, ctx: &Context<'_>, dog_ids: Vec<ID>) -> Result<Walk> {
        let app_ctx = ctx.data::<AppContext>()?;
        let user = user_service::get_or_create_user(&app_ctx.db, &app_ctx.cognito_sub).await?;
        let ids: Result<Vec<uuid::Uuid>, _> = dog_ids.iter()
            .map(|id| uuid::Uuid::parse_str(id))
            .collect();
        let ids = ids.map_err(|e| async_graphql::Error::new(e.to_string()))?;
        Ok(walk_service::start_walk(&app_ctx.db, user.id, ids).await?)
    }

    async fn finish_walk(&self, ctx: &Context<'_>, walk_id: ID) -> Result<Walk> {
        let app_ctx = ctx.data::<AppContext>()?;
        let user = user_service::get_or_create_user(&app_ctx.db, &app_ctx.cognito_sub).await?;
        let id = uuid::Uuid::parse_str(&walk_id)
            .map_err(|e| async_graphql::Error::new(e.to_string()))?;
        Ok(walk_service::finish_walk(&app_ctx.db, id, user.id).await?)
    }
}
```

- [ ] **Step 5: テスト実行**

```bash
cd apps/api
TEST_MODE=true cargo test test_start_walk test_start_walk_empty_dogs_returns_error test_finish_walk test_my_walks_query -- --nocapture
```

Expected: 全テスト PASS

- [ ] **Step 6: コミット**

```bash
git add apps/api/src/services/walk_service.rs apps/api/src/graphql/query/walk.rs apps/api/src/graphql/mutation/walk.rs apps/api/tests/test_walk.rs
git commit -m "feat(api): add Walk CRUD with multi-dog support and myWalks query"
```

---

## Task 10: WalkPoints DynamoDB

**Files:**
- Create: `apps/api/src/services/walk_points_service.rs`

- [ ] **Step 1: LocalStackにDynamoDBテーブルを作成**

```bash
AWS_DEFAULT_REGION=ap-northeast-1 \
AWS_ACCESS_KEY_ID=test \
AWS_SECRET_ACCESS_KEY=test \
aws dynamodb create-table \
  --table-name WalkPoints \
  --attribute-definitions \
    AttributeName=pk,AttributeType=S \
    AttributeName=sk,AttributeType=S \
  --key-schema \
    AttributeName=pk,KeyType=HASH \
    AttributeName=sk,KeyType=RANGE \
  --billing-mode PAY_PER_REQUEST \
  --endpoint-url http://localhost:4566
```

Expected: `{ "TableDescription": { "TableName": "WalkPoints", ... } }`

- [ ] **Step 2: walk_points_service.rsのテストを書く**

`test_walk.rs` に以下を追加:

```rust
#[tokio::test]
async fn test_add_walk_points() {
    let client = common::test_client().await;
    let dog_id = create_test_dog(&client).await;

    let start_res = client
        .post("/graphql")
        .header("Authorization", "Bearer test-token")
        .json(&serde_json::json!({
            "query": format!(r#"mutation {{ startWalk(dogIds: ["{}"]) {{ id }} }}"#, dog_id)
        }))
        .send().await.unwrap();
    let start_body: serde_json::Value = start_res.json().await.unwrap();
    let walk_id = start_body["data"]["startWalk"]["id"].as_str().unwrap();

    let res = client
        .post("/graphql")
        .header("Authorization", "Bearer test-token")
        .json(&serde_json::json!({
            "query": format!(r#"mutation {{
                addWalkPoints(walkId: "{}", points: [
                    {{ lat: 35.6762, lng: 139.6503, recordedAt: "2026-03-21T10:00:00Z" }},
                    {{ lat: 35.6763, lng: 139.6504, recordedAt: "2026-03-21T10:00:05Z" }}
                ])
            }}"#, walk_id)
        }))
        .send().await.unwrap();
    let body: serde_json::Value = res.json().await.unwrap();
    assert_eq!(body["data"]["addWalkPoints"], true);
}
```

- [ ] **Step 3: walk_points_service.rsを作成**

```rust
// apps/api/src/services/walk_points_service.rs
use aws_sdk_dynamodb::{
    Client as DynamoClient,
    types::{AttributeValue, WriteRequest, PutRequest},
};
use uuid::Uuid;
use crate::graphql::types::walk::WalkPoint;
use crate::error::AppError;

pub struct WalkPointInput {
    pub lat: f64,
    pub lng: f64,
    pub recorded_at: String,
}

/// バッチ書き込み: DynamoDBは1回のバッチで最大25件まで
pub async fn add_walk_points(
    client: &DynamoClient,
    table_name: &str,
    walk_id: Uuid,
    points: Vec<WalkPointInput>,
) -> Result<bool, AppError> {
    if points.len() > 200 {
        return Err(AppError::BadRequest("batch size must be <= 200".to_string()));
    }

    let pk = format!("WALK#{}", walk_id);
    let all_requests: Vec<WriteRequest> = points
        .iter()
        .enumerate()
        .map(|(seq, point)| {
            let item = std::collections::HashMap::from([
                ("pk".to_string(), AttributeValue::S(pk.clone())),
                ("sk".to_string(), AttributeValue::S(format!("PT#{:06}", seq))),
                ("lat".to_string(), AttributeValue::N(point.lat.to_string())),
                ("lng".to_string(), AttributeValue::N(point.lng.to_string())),
                ("recorded_at".to_string(), AttributeValue::S(point.recorded_at.clone())),
            ]);
            WriteRequest::builder()
                .put_request(PutRequest::builder().set_item(Some(item)).build().unwrap())
                .build()
        })
        .collect();

    // DynamoDBは1バッチ最大25件のため分割送信
    for chunk in all_requests.chunks(25) {
        client
            .batch_write_item()
            .request_items(table_name, chunk.to_vec())
            .send()
            .await
            .map_err(|e| AppError::Internal(e.to_string()))?;
    }

    Ok(true)
}

/// WalkPointsをDynamoDBから取得してrecorded_at順にソート
pub async fn get_walk_points(
    client: &DynamoClient,
    table_name: &str,
    walk_id: Uuid,
) -> Result<Vec<WalkPoint>, async_graphql::Error> {
    let pk = format!("WALK#{}", walk_id);
    let result = client
        .query()
        .table_name(table_name)
        .key_condition_expression("pk = :pk")
        .expression_attribute_values(":pk", AttributeValue::S(pk))
        .send()
        .await
        .map_err(|e| async_graphql::Error::new(e.to_string()))?;

    let mut points: Vec<WalkPoint> = result
        .items()
        .iter()
        .filter_map(|item| {
            let lat = item.get("lat")?.as_n().ok()?.parse().ok()?;
            let lng = item.get("lng")?.as_n().ok()?.parse().ok()?;
            let recorded_at = item.get("recorded_at")?.as_s().ok()?.clone();
            Some(WalkPoint { lat, lng, recorded_at })
        })
        .collect();

    // recorded_at でソート
    points.sort_by(|a, b| a.recorded_at.cmp(&b.recorded_at));
    Ok(points)
}
```

- [ ] **Step 4: mutation/walk.rsにaddWalkPointsを追加**

`WalkMutation` の `#[Object]` impl に以下を追加:

```rust
async fn add_walk_points(
    &self,
    ctx: &Context<'_>,
    walk_id: ID,
    points: Vec<WalkPointInput>,
) -> Result<bool> {
    let app_ctx = ctx.data::<AppContext>()?;
    let id = uuid::Uuid::parse_str(&walk_id)
        .map_err(|e| async_graphql::Error::new(e.to_string()))?;
    let point_inputs: Vec<crate::services::walk_points_service::WalkPointInput> = points
        .into_iter()
        .map(|p| crate::services::walk_points_service::WalkPointInput {
            lat: p.lat,
            lng: p.lng,
            recorded_at: p.recorded_at,
        })
        .collect();
    Ok(crate::services::walk_points_service::add_walk_points(
        &app_ctx.dynamo,
        &app_ctx.config.dynamodb_table_walk_points,
        id,
        point_inputs,
    ).await?)
}
```

```rust
// WalkPointInput InputObject (mutation/walk.rsに追加)
#[derive(async_graphql::InputObject)]
pub struct WalkPointInput {
    pub lat: f64,
    pub lng: f64,
    pub recorded_at: String,
}
```

- [ ] **Step 5: テスト実行**

```bash
cd apps/api
TEST_MODE=true cargo test test_add_walk_points -- --nocapture
```

Expected: PASS

- [ ] **Step 6: コミット**

```bash
git add apps/api/src/services/walk_points_service.rs apps/api/src/graphql/mutation/walk.rs
git commit -m "feat(api): add WalkPoints batch write and query via DynamoDB"
```

---

## Task 11: WalkStats クエリ

**Files:**
- Modify: `apps/api/src/graphql/types/dog.rs` (Dog.walkStats resolver は Task 6 で追加済み)
- Test: `apps/api/tests/test_dog.rs` (walkStats テストを追加)

- [ ] **Step 1: test_dog.rsにwalkStatsテストを追加**

```rust
#[tokio::test]
async fn test_walk_stats() {
    let client = common::test_client().await;
    // 犬を作成
    let create_res = client
        .post("/graphql")
        .header("Authorization", "Bearer test-token")
        .json(&serde_json::json!({
            "query": r#"mutation { createDog(input: { name: "StatsDog" }) { id } }"#
        }))
        .send().await.unwrap();
    let create_body: serde_json::Value = create_res.json().await.unwrap();
    let dog_id = create_body["data"]["createDog"]["id"].as_str().unwrap();

    // walkStats はゼロ件で正常に返る
    let res = client
        .post("/graphql")
        .header("Authorization", "Bearer test-token")
        .json(&serde_json::json!({
            "query": format!(r#"{{ dog(id: "{}") {{ walkStats(period: ALL) {{ totalWalks totalDistanceM totalDurationSec }} }} }}"#, dog_id)
        }))
        .send().await.unwrap();
    let body: serde_json::Value = res.json().await.unwrap();
    assert_eq!(body["data"]["dog"]["walkStats"]["totalWalks"], 0);
    assert_eq!(body["data"]["dog"]["walkStats"]["totalDistanceM"], 0);
}
```

- [ ] **Step 2: テスト実行**

```bash
cd apps/api
TEST_MODE=true cargo test test_walk_stats -- --nocapture
```

Expected: PASS (walkStatsリゾルバーはTask 6のDog `#[Object]` implに実装済み)

- [ ] **Step 3: コミット**

```bash
git add apps/api/tests/test_dog.rs
git commit -m "test(api): add walkStats query test"
```

---

## Task 12: 動作確認 + Dockerfile

**Files:**
- Create: `apps/api/Dockerfile`
- Test: `apps/api/tests/test_health.rs`

- [ ] **Step 1: test_health.rsを作成**

```rust
// apps/api/tests/test_health.rs
mod common;

#[tokio::test]
async fn test_health_endpoint() {
    let client = common::test_client().await;
    let res = client.get("/health").send().await.unwrap();
    assert_eq!(res.status(), 200);
    assert_eq!(res.text().await.unwrap(), "ok");
}
```

- [ ] **Step 2: テスト実行**

```bash
cd apps/api
TEST_MODE=true cargo test -- --nocapture 2>&1 | tail -20
```

Expected: 全テスト PASS

- [ ] **Step 3: Docker Composeで全サービス起動して動作確認**

```bash
cd apps/api
docker compose up -d
docker compose logs api --follow &

# ヘルスチェック
curl -s http://localhost:3000/health
```

Expected: `ok`

```bash
# meクエリ (TEST_MODE=true でJWT検証をスキップ)
curl -s -X POST http://localhost:3000/graphql \
  -H "Content-Type: application/json" \
  -d '{"query":"{ me { id displayName dogs { id name } } }"}' | jq .

# 犬を作成
curl -s -X POST http://localhost:3000/graphql \
  -H "Content-Type: application/json" \
  -d '{"query":"mutation { createDog(input: { name: \"Taro\", breed: \"Shiba\" }) { id name breed } }"}' | jq .
```

Expected: `{ "data": { "me": { "id": "...", ... } } }`

- [ ] **Step 4: Dockerfileを作成**

```dockerfile
# apps/api/Dockerfile
# --- Build Stage ---
FROM rust:1-slim-bookworm AS builder
WORKDIR /app
RUN apt-get update && apt-get install -y pkg-config libssl-dev && rm -rf /var/lib/apt/lists/*
COPY Cargo.toml Cargo.lock ./
COPY migration/Cargo.toml ./migration/
COPY src ./src
COPY migration/src ./migration/src
RUN cargo build --release

# --- Runtime Stage ---
FROM debian:bookworm-slim
RUN apt-get update && apt-get install -y ca-certificates libssl3 && rm -rf /var/lib/apt/lists/*
WORKDIR /app
COPY --from=builder /app/target/release/walking-dog-api .
EXPOSE 3000
CMD ["./walking-dog-api"]
```

> **注意**: `rust:1-alpine` はOpenSSLの静的リンクが難しいため、`rust:1-slim-bookworm` を使用する。

- [ ] **Step 5: Dockerビルド確認**

```bash
cd apps/api
docker build -t walking-dog-api:local .
docker run --rm -e DATABASE_URL=... -e TEST_MODE=true -p 3000:3000 walking-dog-api:local
```

- [ ] **Step 6: 最終コミット**

```bash
git add apps/api/Dockerfile apps/api/tests/test_health.rs
git commit -m "feat(api): add Dockerfile and health endpoint, Phase 1 complete"
```

---

## 完了チェックリスト

- [ ] `docker compose up` で全サービス (postgres, localstack, cognito-local, api) が起動する
- [ ] `GET /health` → `"ok"` を返す
- [ ] `me` クエリ → ユーザーを取得/作成し、`dogs` フィールドも返す
- [ ] `createDog` / `updateDog` / `deleteDog` が動作する
- [ ] `BirthDate` のJSONB柔軟保存が動作する (`{year, month}` など部分的なデータ)
- [ ] `startWalk(dogIds: [])` が `BAD_USER_INPUT` エラーを返す
- [ ] `startWalk` → `finishWalk` フローが動作する
- [ ] `myWalks` クエリが `Walk.dogs` フィールドと共に返る
- [ ] `addWalkPoints` でDynamoDBへの書き込みが動作する
- [ ] `walkStats` クエリが動作する
- [ ] `TEST_MODE=true` でJWT検証をスキップできる
- [ ] `COGNITO_ENDPOINT_URL` を設定すると cognito-local の JWKs エンドポイントを使う
- [ ] `AWS_ENDPOINT_URL` がDynamoDB/S3クライアントに反映される
- [ ] 本番用 `Dockerfile` (マルチステージビルド) でイメージがビルドできる
- [ ] 開発用 `Dockerfile.dev` で `cargo watch` ホットリロードが動作する
